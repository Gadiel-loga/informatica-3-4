#include <stdio.h>

int main(void){
int number; //there was a exclamation mark
int test_value; //replace the space with a sign
int example1; //change the number of place (it can't be on the first place)
float int_variables; //write more letters
}
