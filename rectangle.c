#include <stdio.h>

int main(void){

    char width[1000];
    printf("Enter the width for the triangle:");
    scanf("%s", &width);
    printf("Area %s!\n",width);
    char
    int Area = &width;

    scanf("%d", &x);
    printf("Area %d",x);

    char height[1000];
    printf("Enter the height for the triangle:");
    scanf("%s", &number);
    printf("Area %s!\n",number);
}
