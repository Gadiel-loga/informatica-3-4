#include <stdio.h>

int main(void){

    int length;
    int width;
    printf("Enter the length for the triangle: \n");
    scanf("%d", &length);
    printf("Enter the width for the triangle: \n");
    scanf("%d", &width);
    int Area =
}
